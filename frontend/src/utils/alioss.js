// placeholder file for OSS, real credentials must NOT be committed!

export function uploadToOSS(file) {
  console.error("OSS upload not configured! Please configure in production server.");
  return Promise.reject("OSS not configured.");
}

